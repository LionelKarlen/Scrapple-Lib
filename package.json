{
	"name": "scrapple-lib",
	"version": "0.1.0",
	"description": "A library for a game that resembles Scrabble, but totally isn't",
	"main": "index.js",
	"license": "MIT",
	"devDependencies": {
		"husky": "^7.0.0",
		"prettier": "^2.6.2",
		"ts-node": "^10.8.0"
	},
	"scripts": {
		"debug": "ts-node debug.ts",
		"prepare": "husky install",
		"lint": "echo lint",
		"format-check": "yarn prettier --check .",
		"format-write": "yarn prettier --write ."
	},
	"dependencies": {
		"typescript": "^4.6.4"
	}
}
